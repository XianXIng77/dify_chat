spring:
  application:
    name: Dify_Chat_api
  
  # 数据库配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.19.128:3306/dify_chat?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 123

  ai:
    openai:
      api-key: sk-440db490b2224df280344441dc783553
      base-url: https://dashscope.aliyuncs.com/compatible-mode
      chat:
        options:
          model: qwen-plus
          temperature: 0.3
          max-tokens: 1000

# MyBatis-Plus 配置
mybatis-plus:
  # 实体类包路径
  type-aliases-package: org.example.dify_chat_api.entity
  # XML映射文件位置
  mapper-locations: classpath:mapper/*.xml
  configuration:
    # 开启驼峰命名转换
    map-underscore-to-camel-case: true
    # 日志级别
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl